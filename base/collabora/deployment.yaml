apiVersion: apps/v1
kind: Deployment
metadata:
  name: collabora
  namespace: preso
spec:
  replicas: 1
  selector:
    matchLabels:
      app: collabora
  template:
    metadata:
      labels:
        app: collabora
    spec:
      containers:
        - name: collabora
          image: collabora/code
          ports:
            - containerPort: 9980
          env:
            - name: extra_params
              value: "--o:ssl.enable=false --o:net.post_allow.host[0]=.* --o:net.proto=IPv4"
            - name: aliasgroup1
              value: "https://presoai.stallion-ai.in"
            - name: server_name
              value: "collabora.stallion-ai.in"
            - name: DONT_GEN_SSL_CERT
              value: "1"
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: "1000m"
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: collabora
  namespace: preso
spec:
  selector:
    app: collabora
  ports:
    - port: 9980
      targetPort: 9980
